---
target: critique della UI dashboard
total_score: 11
max_score: 40
na_heuristics: 
p0_count: 2
p1_count: 2
timestamp: 2026-08-25T22-31-22Z
slug: ui-src-main-tsx
---
# Critique UI — EdgePoint Quant Lab (ui/src/main.tssx → slug ui-src-main-tsx)

## Design Health Score: 11/40 (Critical — redesign necessario)

| # | Euristica | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 1 | WS senza reconnect/fallback: dati congelati in silenzio |
| 2 | Match System / Real World | 2 | Lessico di dominio ok ma "Recover" ambiguo, risultati in linguaggio di serializzazione |
| 3 | User Control & Freedom | 1 | Cancel senza conferma/undo; cambio run cancella cronologia eventi senza ricaricarla |
| 4 | Consistency & Standards | 2 | hover e selected identici (#1d4ed8); Cancel cromaticamente uguale alle letture |
| 5 | Error Prevention | 0 | Comandi non gateati sullo stato; doppio click su New run = run duplicati |
| 6 | Recognition Rather Than Recall | 2 | Run identificati da run_id grezzo; created_at/updated_at disponibili ma ignorati |
| 7 | Flexibility & Efficiency | 1 | Nessuna scorciatoia, filtro, deep-link; F5 perde la selezione |
| 8 | Aesthetic & Minimalist | 1 | 7 pannelli sempre visibili, 7 empty-state simultanei, muri di JSON |
| 9 | Error Recovery | 1 | Tutti gli errori REST finiscono in console.error (11 occorrenze); retryable ignorato |
| 10 | Help and Documentation | 0 | Nessun tooltip/guida; empty-state vicoli ciechi |

## Verdetto specificità: category-interchangeable
Palette Tailwind-slate default, Inter di sistema, bottoni tutti uguali, risultati come JSON.stringify in <pre>. createRun() POST senza parametri: il core del prodotto (ricerca parametri) non ha superficie UI. Nessun grafico/tabella; h1 "Quant System" ≠ EdgePoint Quant Lab. Detector deterministico: EXIT 0 sia CLI sia overlay runtime (0 findings) — pulizia anti-pattern, non prova di design authored.

## Priority Issues
1. [P0] Fallimento silenzioso endemico — errori REST in console.error, WS senza reconnect, UI che gela i dati senza dirlo. Fix: gestore errori centralizzato con banner/toast, riconnessione WS con backoff + fallback polling, aria-live sul feed. Cmd: /impeccable harden
2. [P0] Comandi non gateati sullo stato + lista stantia — Pause su Completed possibile, sidebar solo manuale. Fix: matrice stato→azioni abilitate, disable durante fetch, refresh lista su eventi WS. Cmd: /impeccable harden
3. [P1] createRun() senza parametri — spazio di ricerca invisibile. Fix: dialog/form di configurazione run. Cmd: /impeccable shape
4. [P1] Cancel senza conferma né gerarchia danger. Fix: conferma + stile distruttivo riservato. Cmd: /impeccable harden (+clarify)
5. [P2] Risultati come JSON in <pre> — equity/trades/ranking senza tabelle o grafici. Fix: tabella ranking, chart equity, tabella trades. Cmd: /impeccable shape

## Persona red flags
- Alex (power user): niente scorciatoie, niente deep-link (F5 = selezione persa), niente copy del run_id, Refresh manuale.
- Sam (a11y): nessun aria-live sul progress; key={idx} rompe la reconciliazione; pre rosso #fecaca comunica errore per tutto il JSON; contrasti ok (~7:1).
- Riley (edge): run_id senza ellipsis sfonda la sidebar da 320px; backend giù = area vuota senza messaggio; doppio click = run duplicati; .pending/.cancelled senza stile; migliaia di nodi DOM nei pre.

## Minor
h1≠brand; payload fields ignorati (best_score_so_far, current/total, worker_id, retryable, timestamps); artifactUrl hack encodeURIComponent; nessuna media query; schema_version/sequence envelope non gestiti.

## Strengths
Pipeline dati onesta e reattiva (WS tipizzato, auto-load 7 endpoint al Completed, buffer 200); status ridondante testo+colore; empty-state espliciti ovunque.

## Provocative questions
1. Se il WebSocket muore al minuto 40 di un run da 6 ore, dopo quanto il ricercatore nota i dati congelati?
2. L'equity curve è l'unica immagine che un quant vuole vedere: perché è un array JSON in un <pre> rosso?
3. Se collegassi questa shell a un altro backend, cambierebbe qualcosa oltre il titolo?
